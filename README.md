# enterprise-semantics-test-probe
Enterprise-Semantics conformance test harness: schema validation, unique-ID check, broken-reference detection, mapping integrity, lifecycle validity, generated-artifact consistency. CI gate for the authority repository.
